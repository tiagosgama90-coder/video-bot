name: Recuperar Horóscopo Diário US

# Push em .github/triggers/disparar-diario-us-recuperacao força nova geração US (forcar=true).
on:
  push:
    branches: [main]
    paths:
      - '.github/triggers/disparar-diario-us-recuperacao'

permissions:
  actions: write
  contents: read

jobs:
  disparar-us:
    runs-on: ubuntu-latest
    steps:
      - name: Forçar Horóscopo Diário US (nova geração)
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'diario-us.yml',
              ref: 'main',
              inputs: { forcar: 'true' },
            });
            core.info('Workflow diario-us.yml disparado com forcar=true.');
